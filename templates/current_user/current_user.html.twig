{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/Reservation/reservation.css') }}">
{% endblock %}

{% block main %}
{% if user == null %}
    <span>Aucun utilisateur connecté</span>
{% else %}
    {{ include('current_user/_form.html.twig', {'button_label': 'Update'}) }}
{% endif %}
{% endblock %}
